# Hand Gesture Recognition using CNN

## Overview

This project is a Convolutional Neural Network (CNN) designed to classify hand gesture images into multiple categories. The model is built using TensorFlow and Keras, and it includes layers such as convolutional layers, max-pooling layers, dropout layers, and fully connected layers.
